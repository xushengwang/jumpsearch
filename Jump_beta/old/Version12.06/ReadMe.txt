Fixed the PPI precursor charge issue

In version V11, it is correctly, however, it was changed incorrectly after version 12

The bug is because there is a variable that confuse the intensity and mz